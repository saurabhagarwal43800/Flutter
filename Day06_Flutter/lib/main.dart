import 'package:flutter/material.dart';
import 'ui/home.dart';

main() {
  runApp(x());
}

// Enabling Hot Reload
class x extends StatelessWidget {
  build(BuildContext context) {
    return MyApp();
  }
}
